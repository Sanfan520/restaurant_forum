<div class="container">

  <h1>Restaurant Backend</h1>

  <div class="row">

    <div class="col-md-12">
      <div>
        <%= link_to 'Restaurant',admin_restaurants_path, :style=>'color: #ffffff; font-size:14px'%> |
        <%#到restaurant index目錄%>
        <%= link_to 'Categories',admin_categories_path, :style=>'color: #ffffff;font-size:14px'%>
        <%# 到category index目錄%>
      </div>
    </br>

      <div>
        <%= link_to 'New Restaurant', new_admin_restaurant_path, class: "btn btn-primary" %>
      </div>
      <br>

      <table class="table table-striped table-bordered">
        <thead>
          <tr>
            <th>#</th>
            <th>Category</th>
            <th>Name</th>
            <th>#</th>
          </tr>
        </thead>
        <tbody>
          <% @restaurants.each do |restaurant| %> <%#整張table是restaurant複數，使用each 讀每一行時，用restaurant單數，代表一筆筆讀出資料 ex:restaurant.name%>
          <tr><%#table row%>
            <th scope="row"><%= restaurant.id %></th><%#table head%>
            <%#通過屬性值 row，表頭行包括的所有表格都將和表頭單元格聯繫起來。%>
            <td>[<%= restaurant.category.name %>]</td><%#table head%> 
            <td><%= restaurant.name %></td>
            <td>
              <%= link_to '<span class="glyphicon glyphicon-search"></span>'.html_safe, admin_restaurant_path(restaurant) %> &nbsp;
              <%= link_to '<span class="glyphicon glyphicon-pencil">'.html_safe, edit_admin_restaurant_path(restaurant) %> &nbsp;
              <%= link_to '<span class="glyphicon glyphicon-trash"></span>'.html_safe, admin_restaurant_path(restaurant), method: :delete, data: { confirm: "Are you sure?"} %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>

      <div class="text-center">
        <%= paginate @restaurants %>
        <%#是 Kaminari 提供的 helper，後面接從 controller 傳進來的實例變數。%>
      </div>

    </div>

  </div>
</div>
